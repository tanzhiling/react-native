import React from 'react';
import {View, Text} from 'react-native';

const My = () => {
  const styles = {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  };
  return (
    <View style={styles}>
      <Text>我的</Text>
    </View>
  );
};

export default My;
