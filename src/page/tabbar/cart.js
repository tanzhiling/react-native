import React from 'react';
import {View, Text} from 'react-native';

const Cart = () => {
  const styles = {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  };
  return (
    <View style={styles}>
      <Text>购物车</Text>
    </View>
  );
};

export default Cart;
